import React from "react";

function AboutPage() {
  return (
    <div>
      <h2>About</h2>
      <p>
        The purpose of this application is to fetch the dimensions, rank and
        category of an Amazon product using an ASIN. The products details are
        then shown on the front end and saved in the server DB.
      </p>
    </div>
  );
}

export default AboutPage;
